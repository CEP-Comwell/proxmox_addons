# Template placeholder for /etc/network/interfaces
# Use with caution. This file is intentionally minimal and should be customized per site.

# The file below is a suggested starting point generated by the proxmox_network_interface_pinning role.
# Edit before enabling in production.

# Loop through generated bridge_assignments if provided in the play vars
{% if bridge_assignments is defined %}
# Example bridge assignments (auto-generated) - edit to your needs
{% for bridge, ifaces in bridge_assignments.items() %}
# {{ bridge }}
{% for iface in ifaces %}
#  auto {{ iface }}
#  iface {{ iface }} inet manual
{% endfor %}
{% endfor %}
{% endif %}
