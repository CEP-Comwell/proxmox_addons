# NEW: Handlers for network_provision role - consolidated from nic_pinning
- name: update initramfs
  shell: update-initramfs -u -k all
  become: true
  ignore_errors: true

- name: restart networking
  shell: systemctl restart networking
  become: true
  ignore_errors: true