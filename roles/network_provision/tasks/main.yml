# Inventory-driven network provisioning. Discovery gathers local interface
# facts, naming/pinning keeps deterministic identifiers (xg*, eth*), and
# bridge configuration is sourced from inventory (`bridges` in group/host
# vars).

- import_tasks: discovery.yml
- import_tasks: naming.yml
- import_tasks: pinning.yml
- import_tasks: bridging_simple.yml
