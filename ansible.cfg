[defaults]
# Ensure Ansible finds the project's top-level roles/ directory when running
roles_path = ./roles:~/.ansible/roles:/usr/share/ansible/roles

# Default inventory (convenience when running from repo root)
inventory = ./inventory

# Optional: control forks and reduce noisy output
forks = 20
host_key_checking = False
