- name: Deploy Vault using Docker Compose
  hosts: docker-hosts
  become: true
  tasks:
    - name: Run docker-compose up
      shell: |
        docker-compose -f edgesec-vault/docker-compose.yml up -d
      args:
        chdir: /tmp/semaphore/project_1/repository_3_template_11
    - name: Print working directory
      command: pwd    